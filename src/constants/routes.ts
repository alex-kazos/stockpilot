/**
 * Application route constants
 * 
 * Centralized location for all route paths used in the application.
 * Makes it easier to maintain and update routes across the app.
 */
export const ROUTES = {
  ROOT: '/',
  AUTH: '/auth',
  DASHBOARD: '/dashboard',
  SETTINGS: '/settings',
} as const;
